<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kubulkup.auth.mapper.AuthMapper">

    <resultMap id="authResultMap" type="com.kbulkup.user.domain.User">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="username" column="username"/>
        <collection property="roles" ofType="java.lang.String">
            <result column="role"/>
        </collection>
    </resultMap>

    <select id="findByEmail" parameterType="string" resultMap="authResultMap">
        SELECT
            u.user_id, u.email, u.password, u.username, r.role
        FROM users u
        LEFT JOIN user_roles ur ON u.user_id = ur.FK1
        LEFT JOIN roles r ON ur.FK2 = r.role_id
        WHERE u.email = #{email}
    </select>



</mapper>
