<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kbulkup.payment.mapper.TraineeTrainingPaymentMapper">

    <!-- 결제 성공 시 enrollments 테이블에 신규 수강 등록 (중복 방지) -->
    <insert id="insertEnrollment">
        INSERT INTO enrollments (user_id, training_id, progress, created_at)
        SELECT #{userId}, #{trainingId}, 0, NOW()
            WHERE NOT EXISTS (
            SELECT 1 FROM enrollments
            WHERE user_id = #{userId} AND training_id = #{trainingId}
                )
    </insert>

    <!-- ✅ 방금 등록한 enrollment_id 조회 -->
    <select id="findEnrollmentId" resultType="long">
        SELECT enrollment_id
        FROM enrollments
        WHERE user_id = #{userId}
          AND training_id = #{trainingId}
        ORDER BY created_at DESC
            LIMIT 1
    </select>

    <!-- ✅ routine_results 초기화 (루틴별 기본 결과 생성) -->
    <insert id="insertRoutineResultsForEnrollment">
        INSERT INTO routine_results (enrollment_id, routine_id, status, pass_fail_result)
        SELECT #{enrollmentId}, r.routine_id, FALSE, NULL
        FROM routines r
        WHERE r.training_id = #{trainingId}
    </insert>

</mapper>

